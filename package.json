{
  "name": "tabib-iq-backend",
  "version": "1.0.0",
  "description": "Backend API for Tabib IQ medical platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build step required for Node.js\"",
    "lint": "echo \"No linting configured\"",
    "health": "curl -f http://localhost:5000/api/health || exit 1",
    "deploy": "git add . && git commit -m 'Deploy update' && git push",
    "clean": "rm -rf node_modules package-lock.json && npm install",
    "update-deps": "npm update && npm audit fix",
    "logs": "railway logs",
    "status": "railway status"
  },
  "keywords": [
    "medical",
    "appointments",
    "healthcare",
    "api"
  ],
  "author": "Tabib IQ Team",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.41.0",
    "cors": "^2.8.5",
    "date-fns": "^2.29.3",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.0",
    "nodemailer": "^6.9.1",
    "twilio": "^4.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
